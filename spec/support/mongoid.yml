development:
  sessions:
    default:
      database: importer
      hosts:
        - localhost:27017
      options:
        safe: true
test:
  sessions:
    default:
      database: importer
      hosts:
        - localhost:27017
      options:
        safe: true